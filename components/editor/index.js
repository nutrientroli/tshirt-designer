import Preview from "../preview";
import TshirtOptions from "../tshirt-options";

const Editor = () => {
  return (
    <div>
      <Preview />
      <TshirtOptions />
    </div>
  );
};

export default Editor;
